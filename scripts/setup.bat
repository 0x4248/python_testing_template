@echo off
echo Setting up virtual environment
python -m venv .
echo Done. Now run the command
echo pip install -r scripts\requirements\requirements.txt 
echo to install dependencies or run
echo pip install -r scripts\requirements\top_ten.txt 
echo to install only the top ten python packages