@echo off
echo Setting up virtual environment
python -m venv .
echo Done. Now run pip install -r scripts\requirements\requirements.txt to install dependencies
echo or pip install -r scripts\requirements\top_ten.txt to install only the top ten python packages